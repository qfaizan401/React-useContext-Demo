import React from 'react'
import Child from './Child'

const Parent = () => {
    return(
        <div>
            Parent Component
            <Child />
        </div>
    )
}

export default Parent