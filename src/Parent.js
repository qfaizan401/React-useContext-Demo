import React from 'react'
import Child from './Child'

const Parent = (props) => {
    return(
        <div>
            Parent Component
            <Child />
        </div>
    )
}

export default Parent